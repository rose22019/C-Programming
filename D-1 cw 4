#include <stdio.h>

int main() {
    int year;
    scanf("%d", &year);
    for (int i = year + 1; ; i++) {
        if (i % 4 == 0) {
            printf("The next leap year is: %d\n", i);
            break;
        }
    }
    return 0;
}
